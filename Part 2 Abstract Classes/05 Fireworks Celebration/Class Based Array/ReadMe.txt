
Add Fireworks as an inherited class does not need to be made if very similar to BALL

Only if the majority of the class is new code

Encapsulating the Triggering specific VOIDs or RETURNs in BALL 
  decreases the required communication between classes AbstractPong.draw()

Fully encapsulated objects with little communciation between objects will help with the Array List